dist/
.expo/
node_modules/
IVAStmp*/
.claude/
# Native folders are generated by `expo prebuild` (CNG) — let EAS build them
# fresh on the server so release signing is wired to EAS credentials.
/android
/ios
*.keystore

# Build artifacts, local test builds, screenshots and CLI logs — not needed
# for the EAS build and were bloating the upload archive (~178 MB).
*.apk
*.aab
/*.png
*.log
eas-list.json
eas-view.json
